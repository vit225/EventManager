spring:
  application:
    name: EventManager
  datasource:
    url: jdbc:postgresql://localhost:5430/postgres
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    url: /openapi.yml